apiVersion: v1
kind: Service
metadata:
  name: {{ include "gmdh.fullname" . }}-kafka
spec:
  selector:
    app.kubernetes.io/component: kafka
  ports:
    - name: external
      port: 9092
      targetPort: 9092
    - name: docker
      port: 29092
      targetPort: 29092
