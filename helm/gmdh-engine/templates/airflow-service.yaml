apiVersion: v1
kind: Service
metadata:
  name: {{ include "gmdh.fullname" . }}-airflow
spec:
  type: NodePort
  selector:
    app.kubernetes.io/component: airflow
  ports:
    - port: 8080
      targetPort: {{ .Values.airflow.webserverPort }}
      nodePort: 30080
